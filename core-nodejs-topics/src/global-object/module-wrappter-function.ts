/*(function (exports,require, module, __filename, __dirname) {

  return module.export
})*/

/*
On Nodejs module, exports , __dirname etc are accesible without importing or declaring it so from where it is coming ? 

Node.js do a wrapping behind the scene inside a iife and then we can access it without declaring it . 
*/

