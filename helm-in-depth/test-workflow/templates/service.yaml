apiVersion: v1
kind: Service
metadata:
  name: {{ include "test-workflow.fullname" . }}
  labels:
    {{- include "test-workflow.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "test-workflow.selectorLabels" . | nindent 4 }}
